{% render '@skip' %}

<div class="au-body">
  {% render '@header--campaign' %}
  {% render "@sub-header--campaign" %}

  <div class="container-fluid full-width">
    <div class="row">
      {% render '@band--featured' %}
      {% render '@band--hotline' %}
      {% render '@band--video-listing' %}
    </div>
  </div>

  {% render '@something-wrong' %}

</div>

{% render '@footer' %}

{% render "@back-to-top" %}
