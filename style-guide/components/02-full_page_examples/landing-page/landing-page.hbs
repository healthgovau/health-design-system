{{ render '@skip' }}

{{ render '@release-status--launch' }}

<div class="au-body">
  {{ render '@header' }}
  {{> @sub-header }}

  <div class="container full-width">
    <div class="row">
      {{ render '@callout--simple' }}
      {{ render '@band--cards-call-to-action' }}
      {{ render '@band--cards-image'}}
      {{ render '@accordion-group' }}
      {{ render '@band--cards-audience' }}
      {{ render '@band--hotline' }}
      {{ render '@band--featured' }}
      {{ render '@band--cards-resources' }}
      {{ render '@band--statistics' }}
      {{ render '@band--cards-image-vertical' }}
      {{ render '@band--video-listing' }}
    </div>
  </div>

  {{ render '@something-wrong' }}

</div>

{{ render '@footer' }}

{{> @back-to-top }}
