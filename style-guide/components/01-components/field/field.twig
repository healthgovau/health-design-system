<div class="health-field{% if field.modifier %} {{field.modifier}}{% endif %}">
  <div class="health-field__label">{{ field.label }}:</div>
  <div class="health-field__item">{{ field.item }}</div>
</div>
